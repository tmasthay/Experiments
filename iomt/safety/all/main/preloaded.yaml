# preloaded
# @VS@ cd _dir && cd ../.. && python main.py --config-name=preloaded

defaults:
  - global
  - .marm2_fine
  - _self_

use_prev_data: true
prev_data_dir: /home/tyler/Documents/.__DONT_DELETE__/marm2_fine
dupe: false

postprocess:
  plt:
    obs:
      frames:
        max_frames: 3
    theme:
      imshow:
        cmap: seismic
