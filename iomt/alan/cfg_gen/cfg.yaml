defaults:
  - _self_
  - static: elastic_landscape
  - resolve_order: ${static}
  - rt: ${static}

import_specs:
  delim: "@"
  key: "__import__"
  ignore_spaces: true

use_prev_data: false
prev_data_dir: null
